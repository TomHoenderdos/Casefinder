# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the rake db:seed (or created alongside the db with db:setup).
#
# Examples:
#
#   cities = City.create([{ name: 'Chicago' }, { name: 'Copenhagen' }])
#   Mayor.create(name: 'Emanuel', city: cities.first)

#Finishing.create([{:name => 'Croco Structure', :search_string => 'Croco'},{:name => 'Snake Structure', :search_string => 'Snake'},{:name => 'Print', :search_string => 'Print'},{:name => 'Gloss', :search_string => 'Gloss'},{:name => 'Other', :search_string => 'Other'}])

# Case.all.each do |c|
# 	Finishing.all.each do |f|
# 		if c.finishing == f.search_string
# 			p c.id
# 			p c.finishing
# 			p f.id
# 			p f.search_string
# 			Finishingcase.create(:case_id => c.id, :finishing_id => f.id)
# 		end
# 	end
# end
